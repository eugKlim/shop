import AuthFormContainer from '../UI/AuthFormContainer';

const TokenVereficationMessage = () => {
  return (
    <AuthFormContainer title="Token verification">
      <div className="text-center">
        <p className="text-gray-600">Token verification...</p>
      </div>
    </AuthFormContainer>
  );
};

export default TokenVereficationMessage;
